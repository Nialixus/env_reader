name: env_reader
description: Read Encrypted Env Configuration or Generate Dart Models from Env Files with Ease.
version: 1.5.0
homepage: https://github.com/Nialixus/env_reader.git

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  args: ^2.4.2
  encryptor: ^0.0.1
  flutter:
    sdk: flutter
  http: ^1.1.0
  universal_file: ^1.0.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
flutter: null